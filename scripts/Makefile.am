dist_bin_SCRIPTS = ipv6_care IPv6_CARE_protect_arguments.sh IPv6_CARE_binary_lookup.sh 

if ENABLE_BASHCOMP
profiledir = $(BASHCOMP_DIR)
profile_DATA = ipv6_care-bashcompletion.sh
endif

EXTRA_DIST = ipv6_care-bashcompletion.sh

